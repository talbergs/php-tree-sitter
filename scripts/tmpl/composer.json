{
  "name": "talbergs/tree-sitter-{{ $grammar }}",
  "keywords": [
    "ffi",
    "libtree-sitter",
    "{{ $grammar }}",
    "tree-sitter-{{ $grammar }}"
  ],
  "version": "NOT SET",
  "license": "BSD-3-Clause",
  "autoload": {
    "files": [
      "grammar.php"
    ]
  },
  "require": {
    "php": ">=8.0.0",
    "ext-ffi": "*"
  }
}
